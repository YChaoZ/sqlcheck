app:
  id: test-app
  version: 1.0.0

# Tab缩进 - ERROR
database:
	host: localhost

spring:
  datasource:
    url: jdbc:mysql://localhost/test

# 缩进错误 非2倍数 - WARNING
test:
  key1: value
   key2: value2

# 重复key路径 - ERROR
app:
  id: first
  id: second

# key路径冲突 - ERROR
server:
  port: 8080
  server:
    port: 9090